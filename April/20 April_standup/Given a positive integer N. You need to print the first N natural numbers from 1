#include<stdio.h>
int main()
{
    int i,n,t;
    scanf("%d",&t);
    while(t-->0)
    {
        scanf("%d",&n);
        for(i=1;i<=n;i++)
        {
            printf("%d ",i);
        }
        printf("\n");
    }
}
